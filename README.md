# recette-cookies
> Une recette gourmande :p
>
## Désindexer un fichier déjà suivi par Git

Pour arrêter de suivre un fichier déjà indexé par Git sans le supprimer localement, on utilise la commande :

```bash
git rm --cached .password-prod
